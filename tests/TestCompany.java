package tests;

public class TestCompany {
	public static void main(String[] args) {

		// Coupon coupon = new Coupon();
		// coupon.setTitle("Casio Digital");
		// coupon.setType(CouponType.WATCHES);
		// coupon.setMessage("discount on Casio digital watches");
		// Calendar cal = Calendar.getInstance();
		// cal.set(2017, 4, 22);
		// Date start = cal.getTime();
		// cal.set(2017, 10, 15);
		// Date end = cal.getTime();
		// coupon.setAmount(100);
		// coupon.setPrice(70);
		// coupon.setStartDate(new java.sql.Date(start.getTime()));
		// coupon.setEndDate(new java.sql.Date(end.getTime()));

		// used the coupon below to create a coupon for TestCustomer class,
		// while the original coupon above
		// is the one that used for testing CompanyFacade here and get deleted
		// in circulation each time.

		// Coupon coupon = new Coupon();
		// coupon.setTitle("nike special");
		// coupon.setType(CouponType.SPORTS);
		// coupon.setMessage("discount on nike");
		// Calendar cal = Calendar.getInstance();
		// cal.set(2017, 6, 2);
		// Date start = cal.getTime();
		// cal.set(2018, 1, 10);
		// Date end = cal.getTime();
		// coupon.setAmount(10);
		// coupon.setPrice(100);
		//
		// coupon.setStartDate(new java.sql.Date(start.getTime()));
		// coupon.setEndDate(new java.sql.Date(end.getTime()));
		//
		// ClientType type = ClientType.COMPANY;
		// CompanyFacade comp;
		// try {
		// comp = (CompanyFacade) CouponSystem.getInstance().login("Nike",
		// "1111", type);
		// comp.createCoupon(coupon);
		// cal.set(2018, 1, 15);
		// end = cal.getTime();
		// coupon.setEndDate(new java.sql.Date(end.getTime()));
		// coupon.setPrice(65);
		// comp.updateCoupon(coupon);
		// System.out.println(comp.getCompany());
		// System.out.println(comp.getCouponsByDate(new Date()));
		// System.out.println(comp.getCouponsByPrice(60));
		// System.out.println(comp.getCouponsByType(CouponType.CAMPING));
		// System.out.println(comp.getCouponsByType(CouponType.ELECTRICITY));
		// System.out.println(comp.getAllCoupons());
		// System.out.println(comp.getCouponsByDate(new Date()));
		// // comp.removeCoupon(coupon);
		// CouponSystem.getInstance().shutdown();
		//
		// } catch (CouponSystemException e) {
		//
		// e.printStackTrace();
		// }

	}
}
